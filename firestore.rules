rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For testing purposes - allow everything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // We'll add proper security rules later after testing is complete
  }
}